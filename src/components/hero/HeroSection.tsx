import Bubbles from "../Bubbles";

const HeroSection = () => {
  return (
    <section
      className="relative min-h-screen w-full bg-main-bg bg-no-repeat bg-cover"
      id="home"
    >
      <Bubbles />
    </section>
  );
};

export default HeroSection;
